function convertir(name:string, ci:string, email:string){
const emailList={
    name:name,
    ci:ci,
    email:email
}
return emailList
}

console.log(convertir('mirian','10503020','mrian@gmail.com'))